#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../component/bin/component-convert" "$@"
  ret=$?
else 
  node  "$basedir/../component/bin/component-convert" "$@"
  ret=$?
fi
exit $ret
